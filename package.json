{
  "name": "odoo-domain-builder",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "A local, visual Odoo domain builder for Chrome.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "package": "npm run build && python3 scripts/package.py"
  }
}
